// métodos comuns de strings
const email = "laracroft@rogermelo.com.br";

// const lastIndexOf = email.lastIndexOf('a')
// const emailSlice = email.slice(4, 9);
const emailReplace = email.replace("o", "j");

console.log(emailReplace, email);
